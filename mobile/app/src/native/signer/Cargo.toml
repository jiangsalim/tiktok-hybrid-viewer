[package]
name = "tiktok-signer"
version = "0.1.0"
edition = "2021"

[lib]
name = "signer_core"
crate-type = ["staticlib", "cdylib"]

[dependencies]
hmac = "0.12"
sha2 = "0.10"
md5 = "0.7"
hex = "0.4"
base64 = "0.21"
rand = "0.8"

[profile.release]
opt-level = "s"
lto = true
strip = true
